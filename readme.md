# What the template spec package could look like

This shows two options for how templates can be authored, and how what the subsequent PUT body would look like. Each directory has a put-body-*.json file.